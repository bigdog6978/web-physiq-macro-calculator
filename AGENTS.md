# Agent notes

## Macro Academy Article Template

Academy articles **must** follow the rule in [`.cursor/rules/macro-academy-article-template.mdc`](.cursor/rules/macro-academy-article-template.mdc) (structure, SEO fields, body outline, cross-links, quality checklist).

The rule auto-attaches via globs when editing under `lib/academy/`, `**/articles.ts`, `app/guides/`, and `components/academy/`.
