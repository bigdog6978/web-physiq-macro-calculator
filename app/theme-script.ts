/** Inline script loaded via `next/script` `beforeInteractive` — prevents theme flash (FOUC). */
export const THEME_INIT_SCRIPT = `(function(){try{var k='physiq-theme';var t=localStorage.getItem(k);var dark=false;if(t==='dark')dark=true;else if(t==='light')dark=false;else if(t==='system')dark=window.matchMedia('(prefers-color-scheme: dark)').matches;else dark=false;var r=document.documentElement;r.classList.toggle('dark',dark);r.style.colorScheme=dark?'dark':'light';var m=document.querySelector('meta[name="color-scheme"]');if(m)m.setAttribute('content',dark?'dark':'light');}catch(e){}})();`;
